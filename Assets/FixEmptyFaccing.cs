using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using QuickFlow;

namespace QuickFlow
{
    public class FixEmptyFaccing : MonoBehaviour
    {

        [ReadOnly]
        public float timer;

        [Separator()]

        public float time;

        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
